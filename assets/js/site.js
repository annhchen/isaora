$(function() {
    var isaora = {
        config: {
        },

        init: function () {
            console.log('initing');
        },

        bindEvents: function () {
        }
    };

    $(document).ready(isaora.init());
});